import 'package:flutter/material.dart';

class ThemeTextButton extends StatelessWidget {
  const ThemeTextButton({super.key});

  @override
  Widget build(BuildContext context) {
    return TextButton(onPressed: () {}, child: Text('hello'));
  }
}
